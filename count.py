minha_lista = [1, 2, 2, 3, 4, 2, 5, 6, 2]

# Elemento que queremos contar
elemento = 2

# Usando o método count() para contar as ocorrências
quantidade = minha_lista.count(elemento)

print(f"A quantidade de vezes que {elemento} aparece na lista é: {quantidade}")
